import './errorMessage.scss'

const ErrorMessage = () => {
    return (
        <div className="error">
            <h2>Error</h2>
            <p>problems with server, try restart page</p>
        </div>
    )
}

export default ErrorMessage